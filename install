#!/usr/bin/tail --lines=+3

### Install script
# Copy-paste output into another terminal to perform the installation

# If you are feeling adventurous, run `sh install` to run the whole install process at once
cabal-dev install --bindir=.
perl sys/static.pl

